from setuptools import setup

setup(name="django-collectfiles",
      packages=[
        "collectfiles",
      ],
      install_requires=[
        "django>=1.4,<1.5",
      ],
      version="0.0.1",
)

